[pytest]
# Quiet the PendingDeprecationWarnings emitted by pact-python's v2 (Consumer/
# Provider/Verifier) API so the test output stays focused on contract results.
filterwarnings =
    ignore::PendingDeprecationWarning
addopts = -ra
